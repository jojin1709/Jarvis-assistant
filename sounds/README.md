# Sounds

JX JARVIS currently generates startup, click, listening, and response effects in the frontend using the Web Audio API.

You can add custom `.mp3` or `.wav` files here later and map them in `frontend/src/lib/sounds.js`.
