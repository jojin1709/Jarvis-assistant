# Assets

Place future visual assets here, such as tray icons, app icons, splash images, or generated bitmap textures.

The current interface renders its holographic HUD using React, TailwindCSS, and Framer Motion, so no external image asset is required for the first run.
